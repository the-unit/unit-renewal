import React from 'react';
import styled from 'styled-components';

const SponsorsContainer = styled.div`
  height: 1000px;
`;

export default function () {
  return <SponsorsContainer>Sponsors Page</SponsorsContainer>;
}
