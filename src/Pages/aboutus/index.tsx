import React from 'react';
import styled from 'styled-components';

const AboutusContainer = styled.div`
  height: 1000px;
`;

export default function () {
  return <AboutusContainer>About Page</AboutusContainer>;
}
